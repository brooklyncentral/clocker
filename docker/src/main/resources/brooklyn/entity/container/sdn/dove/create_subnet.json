[#ftl]
{
  "subnet": {
    "id": "${subnetId}",
    "name": "${subnetName}",
    "network_id": "${networkId}",
    "ip_version": 4,
    "gateway_ip": "${gatewayIp}",
    "cidr": "${networkCidr}",
    "tenant_id": "${tenantId}"
  }
}
